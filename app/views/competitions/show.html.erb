<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @competition.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @competition.user_id %>
</p>

<%= render 'submissions/line'%>

<% if User.find(session[:user_id]).role.to_i == 1 %>
  <%= link_to 'Edit', edit_competition_path(@competition) %> |
<% end %>
<%= link_to 'Back', competitions_path %>

<br></br>
